'use strict';

var Api = angular.module('Api', []);

/*
// register an interceptor service
Api.factory('authInterceptor', [function() {
    return {
        request: function(config) {
            config.headers = config.headers || {};
//            config.headers.Authroization = AuthenticationService.getAuthToken();
            return config;
        }
    };
}]);

// Add an $http interceptor to add Auth token to each request
Api.config(function($httpProvider) {
    $httpProvider.interceptors.push('authInterceptor');
});
*/
